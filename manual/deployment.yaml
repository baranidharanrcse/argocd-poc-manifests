apiVersion: apps/v2
kind: Deployment
metadata:
  name: argocd-poc-manual
  labels:
    app: argocd-poc-manual
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argocd-poc-manual
  template:
    metadata:
      labels:
        app: argocd-poc-manual
    spec:
      containers:
        - name: argocd-poc
          image: myapp:v2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: APP_VERSION
              value: "v2"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
